// Input: Create an array
let array = [1, 2, 3, 4, 5];

// Reverse the array
let reversedArray = array.slice().reverse(); // slice() to avoid modifying the original array

// Print the reversed array
console.log("Reversed array:", reversedArray);
console.log("Array : ",array);
