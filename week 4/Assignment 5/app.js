function printHello() {
  let count = 1; // Initialize a counter
  do {
    console.log("Hello"); // Print "Hello"
    count++; // Increment the counter
  } while (count <= 5); // Continue until count is greater than 5
}

// Call the function to see the output
// printHello();
